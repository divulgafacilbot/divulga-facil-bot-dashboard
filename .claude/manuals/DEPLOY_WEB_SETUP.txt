═══════════════════════════════════════════════════════════════
VERCEL WEB DEPLOYMENT - COMPLETE GUIDE
═══════════════════════════════════════════════════════════════

Generated: 2025-12-24
For project: Posting Bot SaaS

This manual will guide you through deploying your Next.js web app
to Vercel, including environment configuration and custom domains.

═══════════════════════════════════════════════════════════════
OVERVIEW
═══════════════════════════════════════════════════════════════

What you will accomplish:
- Deploy Next.js web app to Vercel
- Configure environment variables
- Set up custom domain (optional)
- Enable automatic deployments

What you will need:
- Vercel account (free tier available)
- GitHub repository with your code
- API URL from Railway deployment
- 10-15 minutes of time

Estimated time: 10-15 minutes

═══════════════════════════════════════════════════════════════
PREREQUISITES
═══════════════════════════════════════════════════════════════

Before starting, ensure:

✓ Your code is committed to Git
✓ Code is pushed to GitHub repository
✓ API is deployed to Railway (see DEPLOY_API_SETUP.txt)
✓ You have your Railway API URL ready

═══════════════════════════════════════════════════════════════
STEP 1: Create Vercel Account
═══════════════════════════════════════════════════════════════

1.1 - Go to: https://vercel.com

1.2 - Click "Sign Up" button (top right)

1.3 - Choose "Continue with GitHub"
      (recommended for automatic deployments)

1.4 - Authorize Vercel to access your GitHub account
      - Click "Authorize Vercel"
      - You may need to enter GitHub password

1.5 - Complete profile setup:
      - Name: Your name
      - Username: Choose username (or use suggested)
      - Click "Continue"

EXPECTED RESULT: You are logged into Vercel dashboard

TROUBLESHOOTING:
- If already have account: just log in
- If GitHub auth fails: try email sign-up instead

───────────────────────────────────────────────────────────────

STEP 2: Import GitHub Repository

2.1 - In Vercel dashboard, click "Add New..."

2.2 - Select "Project"

2.3 - You'll see "Import Git Repository" page

2.4 - Find your repository in the list:
      - Look for: posting-bot-saas (or your repo name)
      - If not visible, click "Adjust GitHub App Permissions"
      - Grant Vercel access to the repository

2.5 - Click "Import" next to your repository

EXPECTED RESULT: Repository import started, configuration page shown

───────────────────────────────────────────────────────────────

STEP 3: Configure Project Settings

3.1 - On the "Configure Project" page, you'll see:

      Project Name:
      - Default: posting-bot-saas
      - You can change this (will be part of URL)
      - Example: posting-bot-web

3.2 - Framework Preset:
      - Should auto-detect: "Next.js"
      - If not, select "Next.js" from dropdown

3.3 - Root Directory:
      - IMPORTANT: Click "Edit"
      - Set to: apps/web
      - This tells Vercel where your Next.js app is in the monorepo

3.4 - Build and Output Settings:
      - Build Command: (auto-detected)
        npm run build
      - Output Directory: (auto-detected)
        .next
      - Install Command: (auto-detected)
        npm install

      Leave these as default unless you have custom needs

3.5 - Node.js Version:
      - Should default to 20.x
      - This matches your local development

EXPECTED RESULT: Project configured correctly

TROUBLESHOOTING:
- If Next.js not detected: ensure apps/web has package.json
- If build settings wrong: manually set Root Directory to apps/web

───────────────────────────────────────────────────────────────

STEP 4: Add Environment Variables

4.1 - On the same configuration page, scroll down to:
      "Environment Variables" section

4.2 - Click "Add" to add variables

4.3 - Add this variable:

      Variable 1 (REQUIRED):
      ────────────────────────────────────
      Name: NEXT_PUBLIC_API_BASE_URL
      Value: https://[your-railway-api-domain]

      Example:
      NEXT_PUBLIC_API_BASE_URL=https://posting-bot-api.up.railway.app

      Environment: Production, Preview, Development (select all)
      ────────────────────────────────────

      CRITICAL: This must match your Railway API URL exactly!
               Include https:// but NO trailing slash

4.4 - If you have other Next.js environment variables, add them now
      (Currently, this project only needs NEXT_PUBLIC_API_BASE_URL)

EXPECTED RESULT: Environment variables added

TROUBLESHOOTING:
- If unsure of API URL: check Railway dashboard for your API service
- Make sure it starts with https:// (not http://)

───────────────────────────────────────────────────────────────

STEP 5: Deploy

5.1 - After configuring everything, click "Deploy" button

5.2 - Vercel will start the deployment process:
      - Building... (this may take 2-5 minutes)
      - You'll see real-time build logs

5.3 - Watch the logs for:
      ✓ Dependencies installed
      ✓ Next.js build started
      ✓ Compiled successfully
      ✓ Collecting page data
      ✓ Generating static pages
      ✓ Build completed

5.4 - If build succeeds, you'll see:
      "Congratulations! Your project has been deployed."

5.5 - Vercel will show:
      - Preview URL
      - Production URL
      - Deployment details

EXPECTED RESULT: Deployment successful

TROUBLESHOOTING:
- If build fails: read error logs carefully
- Common issues: TypeScript errors, missing dependencies
- Fix in code, commit, push (Vercel auto-redeploys)

───────────────────────────────────────────────────────────────

STEP 6: Get Your URLs

6.1 - After successful deployment, you'll see:

      Production URL:
      https://posting-bot-web.vercel.app
      (or similar - "posting-bot-web" is your project name)

6.2 - Copy this URL - you'll need it for:
      - Railway API environment variable (APP_BASE_URL)
      - Testing
      - Sharing with users

6.3 - Test the URL:
      - Click on the production URL
      - Your Next.js app should load
      - Check that it connects to API

EXPECTED RESULT: Web app is live and accessible

───────────────────────────────────────────────────────────────

STEP 7: Update API Environment Variable

7.1 - Go back to Railway dashboard

7.2 - Click on your API service

7.3 - Go to "Variables" tab

7.4 - Find APP_BASE_URL variable

7.5 - Update it to your Vercel URL:
      APP_BASE_URL=https://posting-bot-web.vercel.app

7.6 - Save the variable

7.7 - Railway will automatically redeploy API with new variable

7.8 - Wait for API redeployment (1-2 minutes)

EXPECTED RESULT: API knows the correct frontend URL for CORS and redirects

───────────────────────────────────────────────────────────────

STEP 8: Test Full Integration

8.1 - Open your Vercel URL in browser:
      https://posting-bot-web.vercel.app

8.2 - Test API connection:
      - Open browser console (F12)
      - Try to sign up or log in
      - Check Network tab for API requests
      - Verify requests go to your Railway API
      - Check for CORS errors (should be none)

8.3 - Test features:
      - User registration
      - Login
      - Dashboard loads
      - Any other functionality

8.4 - If everything works:
      🎉 Congratulations! Your app is fully deployed!

EXPECTED RESULT: Web app and API communicating correctly

TROUBLESHOOTING:
- If CORS errors: verify APP_BASE_URL in Railway matches Vercel URL
- If API requests fail: check NEXT_PUBLIC_API_BASE_URL in Vercel
- If pages don't load: check build logs for errors

═══════════════════════════════════════════════════════════════
STEP 9: Configure Custom Domain (Optional)
═══════════════════════════════════════════════════════════════

If you want to use your own domain (e.g., app.yourdomain.com):

9.1 - In Vercel dashboard, go to your project

9.2 - Click "Settings" tab

9.3 - Click "Domains" in left sidebar

9.4 - You'll see current Vercel domains

9.5 - Click "Add" button

9.6 - Enter your domain:
      Examples:
      - app.yourdomain.com (subdomain)
      - yourdomain.com (root domain)
      - www.yourdomain.com (www subdomain)

9.7 - Click "Add"

9.8 - Vercel will show DNS records to add:

      For subdomain (app.yourdomain.com):
      ──────────────────────────────────────
      Type: CNAME
      Name: app
      Value: cname.vercel-dns.com
      ──────────────────────────────────────

      For root domain (yourdomain.com):
      ──────────────────────────────────────
      Type: A
      Name: @
      Value: 76.76.21.21
      ──────────────────────────────────────

9.9 - Go to your domain registrar (GoDaddy, Namecheap, etc.)

9.10 - Add the DNS records exactly as shown by Vercel

9.11 - Wait for DNS propagation (5 minutes to 48 hours)
       Usually takes 10-30 minutes

9.12 - Vercel will automatically verify and issue SSL certificate

9.13 - Once verified, your app will be accessible at:
       https://app.yourdomain.com

9.14 - Update APP_BASE_URL in Railway to your custom domain:
       APP_BASE_URL=https://app.yourdomain.com

EXPECTED RESULT: Custom domain working with SSL

═══════════════════════════════════════════════════════════════
VALIDATION CHECKLIST
═══════════════════════════════════════════════════════════════

Before considering deployment complete, verify:

✓ Code pushed to GitHub
✓ Vercel account created
✓ Project imported from GitHub
✓ Root Directory set to apps/web
✓ NEXT_PUBLIC_API_BASE_URL environment variable set
✓ Build completed successfully
✓ Deployment successful
✓ Production URL accessible
✓ Web app loads correctly
✓ API requests working (check Network tab)
✓ No CORS errors
✓ APP_BASE_URL updated in Railway
✓ Login/signup working
✓ Dashboard accessible
✓ Custom domain configured (if applicable)

═══════════════════════════════════════════════════════════════
REFERENCE
═══════════════════════════════════════════════════════════════

Your Web URLs:
- Vercel Domain: https://posting-bot-web.vercel.app
- Custom Domain: https://app.yourdomain.com (if configured)

Vercel Resources:
- Dashboard: https://vercel.com/dashboard
- Docs: https://vercel.com/docs
- Support: https://vercel.com/support

Environment Variables:
- NEXT_PUBLIC_API_BASE_URL (required)

Build Configuration:
- Root Directory: apps/web
- Framework: Next.js
- Node Version: 20.x

═══════════════════════════════════════════════════════════════
END OF MANUAL
═══════════════════════════════════════════════════════════════
